// AimSection.js
import React from 'react';

const AimSection = () => {
  return (
    <div>
      <h2>Implementation of UP/DOWN counter using following input specification
Two General Purpose I/O Pins as Control Inputs,
One Interrupt Pin as a Control Pin</h2>
      <p>
      To design and implement an UP/DOWN counter using two General Purpose I/O (GPIO) pins as control inputs and one interrupt pin as a control pin.      </p>
    </div>
  );
};

export default AimSection;