## AimSection.js

```jsx
import React from 'react';

const AimSection = () => {
  return (
    <div>
      <h2>Interface an ADC with 8051 Microcontroller and Show the Corresponding Output in Two Seven Segment Displays.</h2>
      <p>
        To interface an Analog-to-Digital Converter (ADC) with an 8051 microcontroller and display the digital output on two seven-segment displays.
      </p>
    </div>
  );
};

export default AimSection;
